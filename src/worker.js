import{scanDomain,scanHostDetails}from'../lib/scanner.mjs';
export default{async fetch(request,env){const url=new URL(request.url);if(url.pathname==='/api/scan'||url.pathname==='/api/details'){if(request.method!=='GET')return Response.json({error:'Method not allowed.'},{status:405});try{const result=url.pathname==='/api/scan'?await scanDomain(url.searchParams.get('domain')):await scanHostDetails(url.searchParams.get('hostname'),url.searchParams.get('domain'));return Response.json(result,{headers:{'Cache-Control':url.pathname==='/api/scan'?'public, max-age=300':'public, max-age=900','Access-Control-Allow-Origin':'*'}});}catch(error){return Response.json({error:error.message||'Scan failed.'},{status:400});}}return env.ASSETS.fetch(request);}};
